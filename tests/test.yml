---

- hosts: localhost
  remote_user: root

  vars:
    automysqlbackup_username: 'admin'
    automysqlbackup_password: 'travis'
    
  roles:
    - role_under_test
    - ansible-role-automysqlbackup
  vars:
    automysqlbackup_enable_pre_hook: yes
    automysqlbackup_enable_post_hook: yes
